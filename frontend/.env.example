# API Configuration
VITE_API_URL=http://localhost:5000/api

# RSA Public Key for Credential Encryption
# Generate this by running in backend folder: npm run generate-keys
# Only the PUBLIC key goes here - it's safe to include in frontend
VITE_RSA_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----\n"
